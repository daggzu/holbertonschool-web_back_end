#!/usr/bin/env python3
"""Module creates type-annotated function that takes a float argument
and returns string representation of the float."""
def to_str(n:float) -> str:
    """function that returns a string rep of float"""
    return str(n)
